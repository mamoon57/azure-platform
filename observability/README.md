# Observability

Log Analytics + App Insights. Dashboards for a quick look, alerts when error rate spikes, KQL for digging into slow requests.

Terraform for workspace, app insights, action group. Sample dashboard JSON + a few KQL queries I actually run.
